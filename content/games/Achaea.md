# Commands

`ab <skillname>` to see abilities
`help <skillname>` for general info
`tasks` for things to do
`info here` for a list of objects
`greet` talk to NPC
`ask <denizen> grading` to advance kaido belt

## Monk combat

### Stances

`hrs` horse

### Commands

`combo` unclear how to use
`hook` basic punch
`jab` PVP punch?
`snk` snapkick - basic kick

### Macros?

Combat prep
- Enter stance
- cast toughness
- Cast `vitality`

During combat
- 